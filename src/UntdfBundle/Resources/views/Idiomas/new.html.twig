{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
   <div class ="card">
         <h3 class="card-header text-white bg-primary mb-3">Nuevo Idioma</h3>
            <div class="card-body">
           
  <ul>
    
    {{ form_start(form) }}
        {{ form_widget(form) }}
       <li> <input type="submit" value="Create" class="btn btn-success"/></li>
    {{ form_end(form) }}

  
        <li>
            <a href="{{ path('idioma_index') }}" class="btn btn-primary"> Volver a la lista</a>
        </li>
    </ul>
</div>
</div>
{% endblock %}
