{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
   <div class ="card">
         <h3 class="card-header text-white bg-primary mb-3">Nuevo Libro</h3>
            <div class="card-body">
           
  <ul>
    
    {{ form_start(form) }}
    

       
        {{ form_widget(form) }}
         {{ form_row(form.foto) }}
       <li> <input type="submit" value="Create" class="btn btn-success"/></li>
    {{ form_end(form) }}

  
        <li>
            <a href="{{ path('libro_index') }}" class="btn btn-primary"> Volver a la lista</a>
        </li>
    </ul>
</div>
</div>
{% endblock %}
