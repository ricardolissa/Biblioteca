{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
 <div class ="card">
   <h3 class="card-header text-white bg-primary mb-3" >Editar Categoria</h3>
   <div class="card-body">
    
    <ul>
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <li> <input type="submit" value="Edit" class="btn btn-warning"/>
            {{ form_end(edit_form) }}
        </li>
        
        
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </li>
        <li>
           <a href="{{ path('categoria_index') }}" class="btn btn-primary"> Volver a la lista</a>
       </li>

   </ul>
   
</div>
</div>
</div>
{% endblock %}