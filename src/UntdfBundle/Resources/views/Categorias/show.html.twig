{% extends 'base.html.twig' %}

{% block body %}
  
    <div class="container">
        <div class="card">
            <h3 class="card-header text-white bg-primary mb-3">{{ categoria.nombre }}</h3>
            <div class="card-body">
               
    
       
            </div>
            <a href="{{ path('categoria_index') }}" class="btn btn-primary"> Volver a la lista</a>
        </div>
    </div>


{% endblock %}
